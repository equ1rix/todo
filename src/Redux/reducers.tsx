interface AppState {}
const initialState: AppState = {};
const appReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default appReducer;